# GlassChat 超精细组件+图标位置层级详解（逐区域+逐图标）

## 整体顶层骨架（嵌套关系锚定）

```Plain
body（全屏居中）
└── .chat-window（主窗口，所有元素父级）
    └── .chat-container（水平Flex：左栏 + 右区）
        ├── 左侧.sidebar（固定280px，垂直排布）
        └── 右侧.right-container（剩余全屏，垂直排布）
            ├── 顶部.window-header（标题栏46px高）
            └── 底部.chat-main（聊天主体）
```

## 一、左侧边栏 `.sidebar`【从上到下+图标精准定位】

1. **顶部第一个：新对话按钮** **`.new-chat-btn`**
   1. 内部布局：左图标 + 右文字
   2. 图标位置：按钮内部**最左侧**，文字靠左居中对齐，图标固定16×16
2. **中部固定菜单：3个** **`.sidebar-item`****（技能/应用/自动化）**
   1. 每一项统一布局：**左SVG图标 + 右侧文字**
   2. 图标：居每项左侧居中，18×18，和文字垂直中线对齐
3. **最近对话分区** **`.sidebar-section`**
   1. 上层：`.section-title` 纯文字（无图标）
   2. 下层：2条历史对话 `.sidebar-item`
      - 每条：**左时钟SVG图标 + 右侧对话标题**，图标靠左、垂直居中
4. **侧边栏底部** **`.sidebar-footer`****（底部分割线上方）**
   1. 整体水平均分：**左设置图标按钮 + 右三点更多按钮**
   2. 两个按钮：左右两端对齐，图标居中内嵌在32×32按钮容器里

## 二、右上角标题栏 `.window-header`【从左→右 逐图标拆解，核心精细区】

标题栏是**水平单行布局，高度固定46px**，元素顺序严格从左到右：

1. **最左端：交通灯** **`.traffic-lights`**
   1. 3个圆点图标：红→黄→绿，紧密靠左排列，间距7px，全部垂直居中
2. **紧接交通灯：侧边栏切换按钮** **`.column-view-btn`**
   1. 内含列视图SVG图标，图标居中在28×28按钮内，垂直和标题栏中线对齐
3. **绝对居中悬浮：窗口标题** **`.window-title`**
   1. 不受左右元素影响，纯居中定位
   2. 布局：**左星星SVG图标 + 右侧GlassChat文字**，图标+文字整体居中
4. **标题栏最右侧：****`.header-actions`** **操作区**
   1. 第一个：搜索图标按钮（SVG居中内嵌）
   2. 第二个：用户头像圆形（纯图形，无图标，居操作区最右端）

## 三、右侧聊天主区 `.chat-main`（上分消息+下输入）

### 3.1 上方：消息列表 `.messages-list` 图标&组件关系

1. **AI消息** **`.message-incoming`****（靠左）**
   1. 最左侧：AI头像圆（内嵌人形SVG图标，居中）
   2. 右侧：消息气泡+时间戳，头像和气泡顶部对齐
2. **用户消息** **`.message-outgoing`****（靠右，反向布局）**
   1. 最右侧：我的头像圆（纯色底无内嵌图标）
   2. 左侧：消息气泡+时间戳，头像和气泡顶部对齐

### 3.2 下方：输入区域 `.input-area`【图标最多，逐行抠细节】

1. **输入区顶部：****`.input-header`**
   1. 左侧：`.model-selector` 模型选择栏
      - 内部：**上多层菱形SVG图标 → 文字Glass-4 Turbo → 下箭头下拉SVG图标**，三者水平居中排布
   2. 右侧空白，无元素
2. **悬浮定位：****`.float-add`** **加号按钮**
   1. 绝对定位：输入框右上角叠加，+号图标居中在圆形按钮内
3. **核心输入包裹层** **`.input-wrap`****（输入框主体）**
   1. 第一行 `.input-row` 水平排布：
   2.  ① 最左：附件图标按钮（回形针SVG居中）
   3.  ② 中间：textarea输入框（纯文本，无图标）
   4.  ③ 右侧操作组：表情图标按钮 → 发送按钮
      - 表情：笑脸SVG居中内嵌
      - 发送：纸飞机SVG，居中在圆形发送按钮内
4. **最底部** **`.input-footer`**
   1. 纯文字提示，**无任何图标**，左右两段文字分居两端

## 四、全局图标统一对齐规则（关键隐藏关系）

1. 所有菜单栏、侧边栏条目图标：**统一垂直中线对齐文字**
2. 标题栏所有图标/按钮：**统一46px标题栏内垂直居中**
3. 所有按钮内嵌SVG：**严格居中在按钮容器内（宽高铺满居中）**
4. 消息头像图标：**圆形容器内绝对居中**
5. 悬浮按钮（加号）：**叠加在输入层右上角，不挤压原有布局**

## 极简动线复盘（一眼看懂所有图标流向）

1. 左栏：左图标+右文字 贯穿所有菜单/对话项；底部分左右双图标按钮
2. 顶栏：左交通灯→切换图标→中星星标题图标→右搜索图标+头像
3. 消息区：左AI头像图标 / 右用户头像
4. 输入区：顶模型双图标→右上悬浮加号→左附件图标→右表情+发送飞机图标